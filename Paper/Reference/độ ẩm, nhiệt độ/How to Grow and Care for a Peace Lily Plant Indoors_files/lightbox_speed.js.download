var DIGIOH_LOADER = DIGIOH_LOADER || {};
(function (digioh_loader) {
    if (digioh_loader.loaded) { return; }
    digioh_loader.loaded = true;
    digioh_loader.loads = digioh_loader.loads || {};
    function getPromise (doc) {
        let ld = digioh_loader.loads;
        if (ld[doc]) {
            return ld[doc];
        }
        let p = {};
        p.promise = new Promise((res, rej) => {
            p.resolveCallback = res;
            p.rejectCallback = rej;
        });
        ld[doc] = p;
        return p;
    }
    digioh_loader.getPromise = getPromise;
    const srcPath = "//forms.bhg.com/w37htfhcq2/vendor/90ca8da8-a24f-4f3e-b716-8138fe220538/";

    function createScriptElement(src, id) {
        var e = document.createElement('script');
        e.type = 'text/javascript';
        e.async = true;
        e.fetchPriority = "high";
        e.src = src;
        if (id) { e.id = id; }
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(e, s);
    }

    function loadScript(doc, filename, cb) {
        if (!digioh_loader.loads[doc]) {
            getPromise(doc);
            let src = `${srcPath}${filename}?cb=${cb}`;
            createScriptElement(src);
        }
    };

    function sendPV() {
        try {
            window.SENT_LIGHTBOX_PV = true;

            var hn = 'empty';
            if (window && window.location && window.location.hostname) {
                hn = window.location.hostname;
            }

            var i = document.createElement("img");
            i.width = 1;
            i.height = 1;
            i.src = ('https://forms.bhg.com/w37htfhcq2/z9g/digibox.gif?c=' + (new Date().getTime()) + '&h=' + encodeURIComponent(hn) + '&e=p&u=44283');
        }
        catch (e) {
        }
    };

    function jsonp(src, callback) {
        const id = "__dgo" + Math.random().toString(36).replace(/^0\./, "");
        const prefix = src.includes("?") ? "&" : "?";
        src += `${prefix}callback=${id}`;
        window[id] = (data) => {
            document.querySelector(`#${id}`)?.remove();
            delete window[id];
            callback(data);
        };
        createScriptElement(src, id);
    }

    function initApi() {
        let c = localStorage.getItem("dgdr") || sessionStorage.getItem("dgdr");
        if (c) {
            if (c.match(/^{.*"env_ver":\s?"digioh-.*}$/i)) {
                const json = JSON.parse(c);
                if (!json.expires || json.expires > Date.now()) {
                    return;
                }
            }
            else {
                return;
            }
        }
        let p = getPromise("dgdr");
        let hostname = window?.location?.hostname || 'empty';
        let uri = `https://forms.bhg.com/a4flkt7l2b/z9gd/90ca8da8-a24f-4f3e-b716-8138fe220538/${hostname}/jsonp/z?cb=${Date.now()}`;
        if (/^true$/.test("false")) {
            uri += "&skip_geo=true";
        }
        jsonp(uri, p.resolveCallback);
    }

    sendPV();
    initApi();

    const qaMode = (window.sessionStorage.getItem('xdibx_boxqamode') == 1 || window.location.href.indexOf('boxqamode') > 0);
    if (qaMode) {
        loadScript("user", "user_qa.js", "639142155307302320");
    loadScript("custom", "custom_qa.js", "018503889CE9B18684AEE212DD3AC147");
    loadScript("main", "main_qa.js", "B1725BA3930C72370FEB86A5D32792BF");
    }
    else {
        loadScript("user", "user.js", "639142155307302320");
    loadScript("custom", "custom.js", "D958DC26D98A1C8DB30C08D2850141AB");
    loadScript("main", "main.js", "CB25253E8F12EA3FB437B5DF60BF4170");
    }

})(DIGIOH_LOADER);